@model SkyInsuranceThird.Models.Vehicle

@{
    ViewBag.Title = "DeleteVehicle";
}

<h2 class="headie">Delete Vehicle</h2>

<div class="profile">
    <h3>Are you sure you want to delete this vehicle from User?</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.HiddenFor(model => model.Id)
            </dt>

            <dt>
                @Html.DisplayNameFor(model => model.Registration)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Registration)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.MakeModel)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.MakeModel)
            </dd>

        </dl>


        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-danger" />

            </div>
        }
    </div>
</div>

@Html.ActionLink("Back to Index", "Index", "SkyInsurance", null, new { @class = "btn btn-primary btn-xs" }) |
@Html.ActionLink("Back To Details", "Details", new { id = Model.UserId }, new { @class = "btn btn-primary btn-xs" })